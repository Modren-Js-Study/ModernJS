## 1. 다음 코드를 완성해주세요

```jsx
function findElement(arr, element) {
  // 1-1. 여기에 코드를 작성해주세요.
  /** 
  findElement함수는 
  주어진 배열에서 특정 값이 있는지 확인하고, 그 값이 있으면 그 위치(index)를 반환하고, 
  없으면 false를 반환하는 함수입니다.
  **/


  // 1-2. 아래의 console.log 결과도 함께 알려주세요.  
}

console.log(findElement(['pink', 'coral', 'cornflowerBlue', 'red', 'deepGreen'], 'red')); 
console.log(findElement(['🍒','🍓','🥝','🍇'], '🍔')); 

```



<br/>
<br/>
<br/>


## 2. 다음 코드를 완성해주세요

```jsx
  class Person {
  // 클래스를 완성해주세요.
  /**
    Person 클래스는 firstName, lastName, age를 인자로 받아서 초기화하고, fullName 메소드를 통해 성과 이름을 반환해야 합니다.
   **/
}

let person1 = new Person('John', 'Doe', 25);
console.log(person1.fullName()); // "John Doe"
console.log(person1.age); // 25

let person2 = new Person('Jane', 'Doe', 30);
console.log(person2.fullName()); // "Jane Doe"
console.log(person2.age); // 30


```